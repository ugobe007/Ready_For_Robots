# Ready For Robots — ChatGPT / Codex project context

Read **AGENTS.md** for the full harness constitution and **CLAUDE.md** for the
per-turn operating summary. This file is the entrypoint when connecting a
ChatGPT / Codex agent to this repository through GitHub — it points at the same
canonical sources of truth so every agent behaves consistently.

> Note: this file was regenerated by the cloud agent to mirror `CLAUDE.md`
> because the locally-added copy was not synced into the agent environment.
> Replace it with your exact content if it differs.

## Canonical sources of truth (read these first)

1. **AGENTS.md** — master charter: roles, priorities, gates, red lines.
2. **CLAUDE.md** — per-turn summary and subagent delegation table.
3. `docs/product_market_fit.md` · `docs/value_first_principle.md` — why users buy.
4. `docs/CAPABILITY_MODEL.md` · `docs/EXPERIMENT_MODE.md` — the Jobs product (`/` = robot URL → credible jobs).
5. `docs/V1_PRETRAFFIC_TEST.md` — the pre-traffic gate and its status.

## Summary for every turn

0. **PMF focus:** the **automated sales pipeline for robot companies** — robot URL → understand → find work → qualify. Drive activated robot-company workspaces, not generic content.
1. **North star order:** names/events → scores → rank → robot specs. Fix junk before ranking tweaks.
2. **One mission per cycle.** Write or follow `missions/YYYY-MM-DD-<slug>/brief.md`.
3. **Observe first:** `python3 scripts/harness_snapshot.py` → read `reports/harness_snapshot_latest.json`.
4. **Verify:** run targeted `pytest`; the frontend lives in `readyforrobots-new/` (Vite + wouter); API is `https://ready-2-robot.fly.dev`.
5. **Do not commit** `reports/` artifacts. Do not change secrets or commit `.env`.

## Current product state (2026-08-17)

- **MATCH TRUTH — PRODUCTION PASS**; **M2 matcher frozen**.
- **Jobs front-door workspace** (`/`) shipped: FIND → RESEARCH → SELECT → REVIEW PROFILE → JOBS; large panel = workspace, narrow rail = navigation/context. Understanding stays frozen; the UI never asserts differentiation it cannot support.
- **Pre-traffic finish line:** live authenticate → restore exact workspace state (the one remaining product proof) → then the release step: deploy the frozen workspace to `readyforrobots.com` (Vercel) → production sanity check → publish C04 → begin traffic.
- Do **not** reopen architecture, matcher, Understanding, or design work before traffic unless a gate exposes a genuine defect.

## Frozen / do-not-touch until traffic

- Matcher / ranking / scoring (`app/services/robot_requirement_match.py`) — M2 frozen.
- Understanding extractors (`docs/robot_understanding_v1.md`) — v1.0 freeze.
- The Jobs workspace UI (`readyforrobots-new/client/src/components/RobotJobsWorkspace.tsx`) — frozen after the integrity pass; log small UX items (e.g. the "Researching…" label during SELECT) as post-launch.
