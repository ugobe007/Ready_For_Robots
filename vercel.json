{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "corepack enable && corepack prepare pnpm@10.4.1 --activate && cd readyforrobots-new && pnpm install --frozen-lockfile",
  "buildCommand": "cd readyforrobots-new && pnpm exec vite build",
  "outputDirectory": "readyforrobots-new/dist/public",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
