# QUALIFY inspect: model burden on the list, placement steps on the card

**Date:** 2026-08-25  
**Type:** build  
**Agents:** ProductSurface + Ontology

## Goal

Step 02 QUALIFY must show the **task-model contract** before a checkbox, and walk **placement steps** (who trains, typical time, data owed, no automatic rebate) on the Job Card. Hardware in the room is not enough. Do not invent dollars.

## Acceptance

1. Collapsed job rows show a short model line (`Site-adapted · 4–12 weeks · integrator`) so checkboxes happen after seeing training burden.
2. Expanded Job Card walks numbered placement steps from the contract (slot → license pack → site adapt → data → workplace qualify → field-data clause). Copy never says certificate and never invents a price.
3. Jobs list hint mentions policy layer and typical time. CRM unlock + Jobs signup taste rows carry the same model line.
4. API `card_contract` includes `list_line` + `steps`. Tests cover CNC vs warehouse list lines and UI copy.

## Out of scope

Matcher retune. SIGNAL / Cal. Model marketplace. Invented FTE or pack prices.
