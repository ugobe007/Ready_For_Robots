import SiteFooter from "@/components/layout/SiteFooter";

/** Home footer — newsletter capture lives in MarketingNewsletterBand above. */
export default function MarketingFooter() {
  return <SiteFooter />;
}
