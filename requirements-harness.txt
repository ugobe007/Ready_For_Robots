# Claude Agent SDK harness — install in a venv used for autonomous missions only.
# For DB intelligence in harness_snapshot, also: pip install -r requirements.txt

claude-agent-sdk>=0.1.0
python-dotenv>=1.0
httpx>=0.27
